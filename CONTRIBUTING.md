# Contributing

Create a focused branch, preserve domain/provider boundaries, never bypass policy, never commit `.env` or database files, and label all simulations. Before a pull request run `npm run lint`, `npm run typecheck`, `npm run test:all`, `npm run build`, `git diff --check`, and review the diff for secrets.
